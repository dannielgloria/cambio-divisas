import React from 'react';
import ReactDOM from 'react-dom';
//import './index.scss';  // Asegúrate de tener este archivo para estilos globales
import App from './App';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);

reportWebVitals();
